plans



الـ Scroll Position: في صفحات الويب الطويلة، لما بترجع لورا، رياكت راوتر بيحاول يرجعك لنفس النقطة اللي كنت واقف فيها في نص الصفحة. في فليت، أحياناً الـ ListView بيرجع لأول الصفحة من فوق.

الـ Dynamic Data Re-fetching: لو الصفحة بتعتمد على بيانات من API، هل المفروض لما أرجع لها ألاقي الداتا القديمة (Instant) ولا المفروض تعمل Refresh؟ رياكت بيدي خيارات للتحكم في ده.

الـ Memory Management: لو الأبلكيشن كبر جداً وبقى فيه 50 فيو في الـ Stack، الرامات هتتقل لأن كل فيو محتفظ بالـ TextFields بتاعته. رياكت راوتر "المهم بيه" بيعمل Unmount للفيوز البعيدة عشان يوفر رامات، ويرجع يبنيها لما











Documentations:

In Fly-in or Fly-out(middlewares) during the navigation process, the Airline engine provides a real-time tracking object injected directly into the page instance.
These properties are only available and valid during the active execution of your middleware guards.

page.flight_radar: Returns the path of the pad currently being processed by the middleware. Think of it as the "Transit port" the plane is currently passing through.

page.is_building: A boolean that returns True only if the flight_radar is currently pointing at the Final Destination of the trip. It returns False if the plane is just passing through a air_zone or a parent path (Transit).

[!IMPORTANT]
These properties are dynamic. If your flight passes through multiple guards, page.flight_radar will update sequentially to reflect the current node's path.

# by default all callable views creation will be created once upon Router initiation

which is most efficient for hardware, if you have a function that really need to be recalled with each update, please use flyState for the variables in that view, and thank you.


func('//////')
func *args are =  ('////',)

Airway('////') #
__new__ *args are =  ('////') # don't register no class in args

obj("//////") # inject self with these shit, string in first place means route
__call__ args = ('//////',)


@func
func *args are =  (<class '__main__.hi'>,)

@Airway # register me
__new__ *args are =  (<class '__main__.hi'>,) # check register

@obj
__call__ args = (<class '__main__.hi'>,) # inject Class with self attr + check register


@func("///////")
func *args are =  ('////////',)
func wrapper cls + *args <class '__main__.hi'> ('////////',)

@Airway("////////")
__new__ *args are =  ('/////////',)
__call__ args (<class '__main__.hi'>,) # inject self attr in this class + check register

@obj("//////") # two calls (fuck)
__call__ args = ('//////',) # inject self with these
__call__ args = (<class '__main__.hi'>,) # inject Class with self attr + check register


can i make the router not in page level? or 2 routers in the page?

كلامك صحيح ودقيق جداً هندسياً. هذه هي القاعدة الذهبية لإدارة الذاكرة ودورة الحياة (Lifecycle) في أنظمة التوجيه الاحترافية، وهي الطريقة الصحيحة لترجمة "نية وإرادة المبرمج" دون أن تفرض عليه قيوداً معقدة.

المبرمج يعبر عن نيته من خلال الرابط (URL) الذي يطلبه، ومهمتك كصانع للمكتبة هي تحويل هذا الرابط إلى قرار بخصوص الـ Instance.

إليك التوجيه الهندسي لكيفية تنفيذ هذه القاعدة وتحديد حالة الكلاس (فعال أم غير فعال) تلقائياً داخل الراوتر:

1. كيف تعرف أن الكلاس له "عنصر فعال"؟ (المحافظة على الـ Instance)
يكون الكلاس فعالاً، ويجب على الراوتر الاحتفاظ بالـ Instance الخاصة به في حالة واحدة:

إذا كان المسار الحالي (Current Route) يقع بالكامل تحت مظلة هذا الكلاس.

مثال تطبيقي لترجمة النية:
إذا كان المبرمج قد عرف Class A ومعه مسارات فرعية:

المستخدم واقف في /user -> الـ Build الرئيسي للكلاس فعال -> الكائن حي.

المستخدم انتقل إلى /user/settings -> الـ Layout الخاص بالكلاس لا يزال ثابتاً، ودالة الابن settings أصبحت فعالة -> الكائن حي.

طالما أن المشغل (Router) يتحرك داخل نطاق روابط هذا الكلاس، فإن الـ Instance تظل محفوظة في قاموس داخلي بالراوتر يسمى مثلاً active_instances.


ok, lets handle the cases, with: MMA=Mark Method As, MCA=Mark Class As, CB=class based, FIM=function is me, FIB=function is build, route = creates new route, ->=returns, CTBC=considered to be cancelled



### 0) Airway, # no obj, register in Airway._pending_airways

# 0.0) Airway('user') call, # route, -> obj

# 0.1) @Airway/class # route, route, MCA child-> class

# 0.2) @Airway('user')/class # route, MCA child-> class

# 0.3) @Airway('user')/func # no obj, FIB route + MMA child -> func



#### 1) subway, no registry in Airway._pending_routes

### 1.0) subway: class & no obj

# 1.0.0) subway(class, parents=[], 'user') # CB route + inject parents, -> obj

# 1.0.1) subway(class)=@subway/class # MCA child-> class

# 1.0.2) @subway(parents=[], 'user')/class # CB route+inject+ MCA child, -> class

## 1.1) subway: class & obj

# 1.1.0) obj.subway(class, parents=[], 'user') # CB route + inject, -> obj

# 1.1.1) obj.subway(class)=@obj.subway/class # CB route+inject+MCA -> class

# 1.1.2)@obj.subway(parents=[], 'user')/class # CB route+inject+MCA -> class

## 1.2)subway: func & no obj

# 1.2.0)subway(func, parents=[], 'user') # FIB route + inject-> obj, 

# 1.2.1)subway(func)=@subway/func # MMA child-> func, 

# 1.2.2)@subway(parents=[], 'user')/func # FIB route + inject, -> func

## 1.3) subway func & obj

# 1.3.0)obj.subway(func, parents=[], 'user') # FIB route + inject, -> obj

# 1.3.1)obj.subway(func)=@obj.subway/func # FIB route+inject+MMA -> func

# 1.3.2)@obj.subway(parents=[], 'user')/func # FIB route+inject+MMA -> func


### 2) layout, build, fly_in, fly_out, register in Airway._pending_airways

## 2.0) layout: class & no obj

# 2.0.0) layout(class, 'user') # CB route (special) -> obj

# 2.0.1) layout(class)=@layout/class # CB route (special) -> class

# 2.0.2) @layout('user')/class # CB route (special) -> class

## 2.1) layout: class & obj

# 2.1.0) obj.layout(class, 'user') # -> Ambiguous, ValueError

# 2.1.1) obj.layout(class)=@obj.layout/class # -> Ambiguous, ValueError

# 2.1.2)@obj.layout('user')/class # -> Ambiguous, ValueError

## 2.2)layout: func & no obj

# 2.2.0)layout(func, 'user') # dict -> dict, 

# 2.2.1)layout(func)=@layout/func # MMA me-> func, 

# 2.2.2)@layout('user')/func # MMA me, -> func

## 2.3) layout func & obj

# 2.3.0)obj.layout(func, 'user') # set method as mine -> obj

# 2.3.1)obj.layout(func)=@obj.layout/func # set method as mine & MMA me -> obj

# 2.3.2)@obj.layout( 'user')/func # set method as mine & MMA me -> obj

